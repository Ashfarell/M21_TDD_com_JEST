const soma = (a, b) => a + b                // função que retorna a soma de dois valores
const dobro = valor => valor *2             // função que retorna o dobro de um valor

module.exports = { soma, dobro }